# P7-T-103 + P7-T-104 (combined) · kind smoke E2E Phase 7 extension + HCCS placement hard-assertion (synthetic ring fixture)

- **Commit**: (this commit)
- **Date**: 2026-05-20
- **Duration**: plan 1d (T103) + 0.5d (T104) / actual ~1d combined(workflow + scripts + fixtures + mock-data set-b + mockjson test)

## Intent

Per phase7-plan.md §4 P7-T-103 + P7-T-104:Phase 7 kind smoke 扩展 — exercise Phase 7 W1 deliverables(T003 schedulerName auto-stamp · T007 NPUSliceTemplate reconciler · T008 HCCS adjacency chart default)在 e2e-kind workflow 中 · 同时引入 synthetic 4-ring multi-ring fixture(`configs/mock-data/set-b-multi-ring/`)让 HCCS scoring 实际跑 4-tier(100/70/30/0)而不是 binary(100/30)。

## Path adaptations + scope

**Plan §4-T103 + T104 Allowed Paths**(全部考虑):

T103:
- ✅ `.github/workflows/e2e-kind.yml`(扩 Phase 7 step · install.sh all + assert.sh)
- ✅ `tests/e2e/kind/phase7/install.sh`(new · reseed mock-data + label nodes + apply NPUSliceTemplate + apply ModelService 4 个 subcommands)
- ✅ `tests/e2e/kind/phase7/assert.sh`(new · T103-1 NST status + T103-2 schedulerName hard-fail + T103-3 chart adjacency + T104-1 multi-ring ResourceSlice + T104-2 placement soft warning)
- ✅ `tests/e2e/kind/phase7/fixtures/npuslicetemplate-qwen-pd.yaml`(matches `npus.huawei.com/slice-template=qwen-8b-pd-pair` label naming convention)
- ✅ `tests/e2e/kind/phase7/fixtures/modelservice-with-template.yaml`(ModelService with slice-template label + fallbackImage=busybox CI bypass)
- ✅ `tests/e2e/kind/phase7/fixtures/multi-ring-nodes.yaml`(documentary node label spec · install.sh `cmd_label_nodes` 实际通过 kubectl label apply)

T104:
- ✅ `configs/mock-data/set-b-multi-ring/meta.json`(NEW dir + scenario descriptor)
- ✅ `configs/mock-data/set-b-multi-ring/npus.json`(16 NPUs · 2 nodes × 8 NPUs × 4 rings)
- ✅ `tests/e2e/kind/phase7/fixtures/multi-ring-nodes.yaml`(same file as T103 — T104 specifies the node labels matching set-b npus.json's ring distribution)
- ✅ `tests/e2e/kind/phase7/assert.sh`(extended — T104-1 / T104-2)
- ✅ `operators/npu-dra-driver/internal/source/mockjson/mockjson_test.go` (extended — 1 new TestLoadSetBMultiRingFixture case asserting 4 distinct rings + 2 nodes)
- ⏭ `docs/known-issues.md`(plan 列了 "soft assertion 关闭" · 实际既有 issues 里没有该 entry · skip)

**Plan T104 acceptance "hard fail on misplacement"的 P3 诚实勘误**:
- Plan §4-T104 acceptance: "prefill Pods placed on ring-0 nodes (per ADR-0010 §5 score tier 100) — workflow fails if any prefill Pod lands on ring-2/3 nodes"
- 现实:Phase 7 W1 PD-pair Pods 不带 `npu.huawei.com/preferred-hccs-ring` annotation(annotation 写入路径 deferred 到 Phase 10/T105-v2 controller wiring)· HCCSTopology Filter 是 permissive(no annotation → 不 filter)· Score 是 "MS label sibling lookup" 但 kind 没真 HCCS · 实际 placement 仍偏 random
- 选择:assert.sh T104-2 段实现为 **"soft warning · 记录 placement 但不 fail"** + comment 明示 "Phase 7 T101 lab smoke verifies real silicon" · 不假装 hard-fail 在 kind 里能成立
- This is P3 verify-before-claim:不为 plan literal compliance 而 ship 假断言

## Debugging trail

- 第一次 mockjson_test 路径错(6 个 ".." vs 5 个)· cwd 是 mockjson/ → repo root 是 5 hops(mockjson/source/internal/npu-dra-driver/operators/) · 改 5 个 ".." 通过
- 无其他 false start。所有 yaml + bash + json 单次 lint 通过(python yaml.safe_load + bash -n)
- 一个犹豫点:`fixtures/multi-ring-nodes.yaml` 是 documentary 而非 `kubectl apply` 直接消费的(因为 kind nodes 已存在 · 不需要 create)· 是否需要存在?保留 · 让 `kubectl get nodes -L ocloud.edge.example.com/synthetic-hccs-ring` 的运维者看 source-of-truth · install.sh 通过 `kubectl label` 实际 apply

## Key decisions

- **Combined commit for T103 + T104**:plan §2 task overview Mermaid 显示 T104 dep on T103("T103 → T104")· 实际两 task allowed paths 大量重叠(install.sh / assert.sh / fixtures/multi-ring-nodes.yaml)· 分两 commits 反而 churn · 一 commit ship 两 task 更清晰
- **T104-2 placement assertion 实现为 soft warning** instead of hard-fail per kind reality(see "P3 诚实勘误" 上)
- **set-b-multi-ring 仅含 meta.json + npus.json** 不像 set-a-small 含全套(clusters/nodes/networkSwitches/etc.):mock-data 的其他文件被 backend / frontend 消费 · npu-dra-driver MockJSONSource 只读 npus.json · set-b 只针对 npu-dra-driver 测试 · 最小 viable fixture 即可
- **install.sh 4 个 subcommands**(reseed-mockdata / label-nodes / apply-template / apply-modelservice + `all` wrapper):mimic phase6/install.sh 的 modular pattern · CI 可串行调用 也可 debug 时单步跑

## Verification

- 存在性:
  - `.github/workflows/e2e-kind.yml` 加 "Phase 7 — kind smoke extension (T103 + T104)" step ✅
  - `tests/e2e/kind/phase7/{install,assert}.sh` (new) ✅
  - `tests/e2e/kind/phase7/fixtures/{npuslicetemplate-qwen-pd,modelservice-with-template,multi-ring-nodes}.yaml` (new) ✅
  - `configs/mock-data/set-b-multi-ring/{meta,npus}.json` (new) ✅
  - mockjson_test.go 加 TestLoadSetBMultiRingFixture ✅
- 完整性(verified by execution):
  - `bash -n install.sh assert.sh` syntax OK ✅
  - 全 YAML + JSON 文件 `python yaml.safe_load` / `json.load` OK ✅
  - `go test ./internal/source/mockjson/... -run TestLoadSetBMultiRingFixture` PASS · 2.410s ✅
  - Workflow extension follows existing Phase 6 step naming + comment style
- 正确性:
  - set-b-multi-ring/npus.json 4 个 ring IDs(0/1/2/3)分布 8+8 NPUs in nodeA+nodeB ✅
  - assert.sh T103-1/2/3 + T104-1/2 顺序 + 实证语义 match plan §4-T103/T104 acceptance(modulo soft-warning instead of hard-fail per P3 诚实)
- 注释:**Phase 7 CI 本地未运行 e2e-kind workflow**(无 kind cluster · GHA workflow 由 PR push 触发)· 当前 commit 落地 bash + yaml + 工作流 step · live e2e validation by next CI run

## Carry-forward

- **Phase 10 demo polish 时 hard-fail placement assertion 上线 candidate**:
  - 增加 inference-operator controller body wiring(P7-T-105 v2):Pod template 自动 stamp `npu.huawei.com/preferred-hccs-ring` annotation 基于 NPUSliceTemplate.spec.composition + ResourceSlice 当前 availability
  - 此时 HCCS Filter 实际 act on annotation · Score 给 same-ring 100 / adjacent-ring 70 · placement 从 random 变 deterministic
  - assert.sh T104-2 段从 soft warning 升 hard fail · 此时 plan §4-T104 acceptance 字面成立
- **set-b-multi-ring 扩展**:Phase 10 时如果需要更多 ring + node 组合(如 16-card 910B-pro · 4×4 ring topology 而非当前 2×2)· 加 set-b-pro-multi-ring 或 set-b-16card 等 sibling fixtures · npus.json 加更多 entry
- **NumaAffinity active assertion** in assert.sh:plan §4-T103 也提到 "NumaAffinity active in profile"· 但 P7-T-002 doc-only fallback → NumaAffinity 仍 placeholder · 不在 chart enabled list · assert.sh skip this · Phase 8 baseline bump 后 hard assertion 上线 candidate
- **TopologyAware Allocator** that reads slice-template ring affinity:Phase 8/9 work that turns "ring affinity is hint" into "ring affinity is constraint" · with allocator-side ring-bin packing across PD-pair · is the prerequisite for kind smoke hard-fail T104-2
