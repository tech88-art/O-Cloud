apiVersion: v1
kind: Service
metadata:
  name: {{ include "o2-dms-adapter.fullname" . }}
  labels:
    {{- include "o2-dms-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "o2-dms-adapter.selectorLabels" . | nindent 4 }}
