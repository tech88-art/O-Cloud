apiVersion: v1
kind: Service
metadata:
  name: {{ include "bare-metal-provisioning-operator.fullname" . }}
  labels:
    {{- include "bare-metal-provisioning-operator.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "bare-metal-provisioning-operator.selectorLabels" . | nindent 4 }}
