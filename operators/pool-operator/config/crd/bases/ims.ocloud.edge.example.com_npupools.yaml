---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: npupools.ims.ocloud.edge.example.com
spec:
  group: ims.ocloud.edge.example.com
  names:
    kind: NPUPool
    listKind: NPUPoolList
    plural: npupools
    shortNames:
    - npup
    singular: npupool
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.npuModel
      name: Model
      type: string
    - jsonPath: .status.totalNPUs
      name: Total
      type: integer
    - jsonPath: .status.healthyNPUs
      name: Healthy
      type: integer
    - jsonPath: .status.allocatedNPUs
      name: Allocated
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NPUPool is the level-3 pool resource: NPU devices grouped by NodePool +
          model. Hierarchy: ClusterPool -> NodePool -> NPUPool -> NPUSlicePool.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NPUPool.
            properties:
              nodePoolRef:
                description: |-
                  NodePoolRef references the parent NodePool (Cluster-scoped). Required
                  so the operator can constrain candidate Nodes when materialising NPUs.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              npuModel:
                description: |-
                  NPUModel is the device model this pool collects. Pools must be
                  homogeneous; multi-model deployments use one NPUPool per model.
                  Example values: "Ascend910B", "Ascend910C".
                minLength: 1
                type: string
              selector:
                description: |-
                  Selector further narrows which NPU devices in the NodePool belong to
                  this pool (e.g. by firmware revision or rack label).
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              sliceStrategy:
                description: |-
                  SliceStrategy names a NPUSlicePool resource (by name; same group) that
                  defines how this pool's NPUs are partitioned. The pool-operator
                  materialises slices according to that strategy.
                minLength: 1
                type: string
            required:
            - nodePoolRef
            - npuModel
            - sliceStrategy
            type: object
          status:
            description: status defines the observed state of NPUPool.
            properties:
              allocatedNPUs:
                description: |-
                  AllocatedNPUs is the count of devices currently bound to one or more
                  active slice allocations.
                format: int32
                minimum: 0
                type: integer
              conditions:
                description: Conditions follow the standard Kubernetes condition convention.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              hccsTopology:
                description: |-
                  HCCSTopology describes the high-speed peer-link fabric across the
                  devices in this pool, if any.
                properties:
                  fabricId:
                    description: |-
                      FabricID is the operator-assigned identifier for the HCCS mesh
                      (e.g. "hccs-rack-1").
                    type: string
                  peerGroups:
                    description: |-
                      PeerGroups lists sets of NPU device IDs that share a single HCCS hop.
                      Devices within a group communicate over HCCS; cross-group traffic falls
                      back to PCIe / RoCE.
                    items:
                      description: HCCSPeerGroup is one HCCS peer set within an NPUPool's
                        fabric.
                      properties:
                        deviceIds:
                          description: DeviceIDs lists the NPU device IDs that share
                            this HCCS peer set.
                          items:
                            type: string
                          minItems: 1
                          type: array
                          x-kubernetes-list-type: set
                        groupId:
                          description: GroupID identifies the peer set within its
                            parent fabric.
                          type: string
                      required:
                      - deviceIds
                      - groupId
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
              healthyNPUs:
                description: |-
                  HealthyNPUs is the count of devices reporting healthy in the last
                  reconcile.
                format: int32
                minimum: 0
                type: integer
              totalNPUs:
                description: TotalNPUs is the number of physical NPU devices currently
                  in the pool.
                format: int32
                minimum: 0
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
